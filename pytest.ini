[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
addopts = -q --cov=app --cov-report=term-missing
markers =
    integration: needs real kafka/redis/postgres (docker-compose)
